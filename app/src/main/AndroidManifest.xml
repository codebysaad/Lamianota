<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidkudus.lamianota">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.auth.remember.RememberPasswordActivity"
            android:parentActivityName=".ui.auth.signin.SigninActivity"
            android:theme="@style/AppStyleNoActionBar" />
        <activity android:name=".ui.keu.insert.InsertUpdateKeuActivity" />
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/account"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.auth.signup.SignupActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.note.insert.InsertUpdateNoteActivity" />
        <activity
            android:name=".ui.auth.signin.SigninActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".reminder.NoteReminderManager"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>